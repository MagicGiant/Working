#include <iostream>
#include <fstream>
#include <vector>
using namespace std;

int main()
{
    ifstream fin ("component.in");
    ofstream fout ("component.out");
    int n, m;
    fin >> n >> m;
    vector <int> dot (n);
    for (int i = 0; i < count; i++)
    {
        /* code */
    }
    
    return 0;
}